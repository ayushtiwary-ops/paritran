# Paritran API runtime dependencies (SPEC section 3: fully pinned).
# Every version below was verified to exist on PyPI via `pip index versions` on 2026-07-11.
fastapi==0.139.0
uvicorn[standard]==0.51.0
pydantic-settings==2.14.2
psycopg[binary]==3.3.4
psycopg-pool==3.3.1
argon2-cffi==25.1.0
prometheus-fastapi-instrumentator==8.0.2
httpx==0.28.1

# Engine: deterministic graph core (SPEC 6.3; pin required for modularity determinism).
networkx==3.4.2

# Engine: legal retrieval stack, InLegalBERT semantic rerank (SPEC 6.7).
torch==2.9.1
transformers==4.57.6

# Engine: identifier extraction and document ingest (SPEC 6.6, 9.1).
spacy==3.8.14
pdfplumber==0.11.10
pytesseract==0.3.13
Pillow==12.3.0
indic-nlp-library==0.92
