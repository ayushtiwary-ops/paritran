# Prometheus scrape config (SPEC section 12). Runs on the internal core network only.
global:
  scrape_interval: 5s

scrape_configs:
  - job_name: api
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8000"]
